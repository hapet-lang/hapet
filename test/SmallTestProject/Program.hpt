using System.Runtime.InteropServices;
using System.Text; 
using SmallTestProject.Test; 
using TestProject.Sub; 

public static class Program
{
	public static int Main(string[] args)
	{
		// DerivedTwice dr = new DerivedTwice();
		// BaseCls bs = dr as BaseCls;
		BaseCls bs = new BaseCls();
		Derived dr222 = bs as Derived;
		if (dr222 == null)
			return 1;
		return 0;
	}
}

public class BaseCls
{
	public int Field1;
}

public class Derived : BaseCls
{
	public int Field2;
}

public class DerivedTwice : Derived
{
	public int Field3;
}